export interface VideoFeedItem {
  id: string;
  titulo: string;
  descripcion?: string;
  instructor?: string;
  videoUrl?: string;
  embedUrl?: string;
  videoId?: string;
  thumbnail?: string;
  subtitle?: string;
}
